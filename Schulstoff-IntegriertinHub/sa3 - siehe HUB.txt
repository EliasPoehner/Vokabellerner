1.  Mailing
welche serverdienste brauchen wir beim Mailing
- Postfix und dovecot
wie heißen die Clients?(mailclient)
- Thunderbird(funktioniert einwandfrei), Evolution(war der Client der nicht mehr mit dem veralteten einstellungen klar gekommen ist, kann leider in Debian13 mit dem Postfach aus  /var/spool/mail/username nicht umgehen), Outlook, Access über webinterfaces
das Protokoll von dovecot?
- imap(Internet message access protocoll)
das Protokoll von Postfix?
- smtp: simple mail transfer protocol

was benötigt ein client? was haben wir ihm gegeben? für thunderbird:
- ports für smtp und imap
- IP oder name für den smtp(steht unter rubrik "outgoing" server) /Imap server(steht unter "incoming")
- username(womit man sich anmelden zb lweikl@edvschule-plattling.de dann username = lweikl)
- passwort

welche Infos hat der dovecot gebraucht zum konfigurieren?
- der dovecot ist schon mit configs gekommen, deshalb etwas schwerer zu erinnern
10_auth.conf = dort sollte drin stehen = plaintext erlaubt ja/nein?
 falls nein SSL keys? (public/private) --> also angeaben zur Authentifizierung[Passwort ggf]
wo ist das Postfach (/var/spool/mail%username&) damit er weiß wo er das hinmuss

wo find ich die dinge?

cd /et/Postfix/ ls -la --> cd dovecot

10-ssl - was steht drin -> keys und welches verschlüsselungsverfahren wird benutzt

10-mail.conf dort sthet drin wo die Inbox zu findne ist mail_inbox_path = /var/mail/%{user}
10-auth.conf = plaintext [auth_allow_cleartext = yes] oder auch mit doppelter Verneinung

cd /etc/Postfix/ nano main.cf
welche einstellungen? haben wir nicht editierd, wie sind die reingekommen? durch ein kommando[wichtiges kommando] dpkg-reconfigure Postfix - da kommen dann die grauen Fenster, wo man schrittweise alles eingeben kann, kann man aber auch über die main.cf ändern
dann steht drin inet_protocol = ip4, mynetworks 10.5.252.0/24(und das sind meine vertrauenswürdigen netze, wichtig, leute die meinen mailserver benutzen wollen, vertrauenswürdig sind, ansonsten gibt's spam), wie heißt er selber, list of Domains= mydestination = mx-lweikl-Plattling.de, (das was nach dem @ dranhämgem darf, diese emails, betrachte ich lokal, also dieser mailserver ist dazuständig, das die email geprüft wird, gibt es die Adresse? ja oder nein und zur Zustellung), securitylevel = security SSL/TSL oder keins und die dazugehörigen keys(wir haben plaintext benutzung gemacht),  (tsl = Transfer secure layer (neuer), SSL = sichere Datenübertragung secure(= Verschlüsselung) socket layer, (SSL = alt), funktionieren beid emit public/private keys), tsl_security_level = may (kann. muss nicht, bei "must" ist es zwingend erforderlich), myorigin=mx.nachname-plattling.de(= der name des mailserver), dns = (spielt zentrale rolle, weil er sagt was aus? zb ich schreib email an dachs-plattling.de, wie findet der mailserver denn den dachs-plattling.de, dns-system ist ein verteilsystem, er fragt, den nächsten ob er was weiß, und der fragt weiter, mein dns-fragt in das system wer ist der mailexanger für dachs-plattling.de? -- die frage auf der kommandozeile? nslookup -type=MX dachs-plattling.de, 
antwort: Server:		127.0.0.1
Address:	127.0.0.1#53

weikl-plattling.de	mail exchanger = 5 mx.weikl-plattling.de.
weikl-plattling.de	mail exchanger = 10 mail.weikl-plattling.de.

#--------------------
iptables
- ipfilter Firewall, da haben wir uns tcptump angeschaut, 
tcpdump -i enp0s8 host 10.5.252.15,[dort kann man anschauen, wo ich zb versucht habe auf eine webseit zuzugreifen] [ unverschlüsselt port 80, verschlüsselt 443,]3-wege handshake(noch anschauen, alle txt Dateien unter iptables!),
- was macht das arp-protokoll? adress Resolution Protokoll, ich frag ins netz, wer hat eine mac adressee zur 10.5.252.5, wie dann mail, gibt es dann zurück
-mit arp -a mach ich das sichtbar, das ist mein Cache

iptables erlauben und sperren von Protokollen und diensten von einzelnen rechner, Reihenfolge wichtig!, wenn ganz unten ein erlaubt alles ist, dann zählt der einzelne rechcner nicht mehr, im iptables pdf steht die Syntax drin,tcp dns unebdingt nochmal drin schauen, auch die scripte vom myfw Syntax und so.... ipverbieten vor weberlauben zb, Reihenfolge! diese regeln von ipverbieten müssen vor dem weberlaub in der "Chain" sein, fr reger würde konkrete regeln zb wollen, sie würde uns nicht die Syntax ausfragen, sie würde befehl vorgeben und wir bestücken sie dann, zb auch die Reihenfolge, also 1 regel ist vorgeschrieben und wir machen dann die Reihenfolge

#-----------------------------
squid(nicht reverse Proxy)
- Der Proxy unter Linux heißt squid
Die squid.conf ist sehr groß, ein ganzes Handbuch, deshalb:
cat squid.conf | grep -v "^#" | grep -v "^$"

dann sieht man die Standardregeln

Sicherungskopie:
cp squid.conf squid.conf.orig
cat squid.conf | grep -v "^#" | grep -v "^$" > squid2.conf

cp squid2.conf squid.conf
-sqid ist ein Forward Proxy
-swid hat 3 aufgaben: (allgeimein ein Forward Proxy hat 3 aufgaben -> im Proxy script, 1.caching, warum macht er das? für statische webinhalte, damt er die anfrage schneller bedienen kann, zb in der automobilbranche öfter Kfz kleinteile aufgerufen, dann geht's einfach schneller, zb frage, was heißt Caching? 2.aufgabe: 1 einzige ip im schulaufhaus für uns alle, er empfängt und verteilt sie, Proxy ist ein Stellvertreter, wenn er die seiten besorgt, muss er auch bestimmen,welche seiten erlaubt sind = 3. , Proxy ist auf schicht 7 (ganz oben, anwendungsschicht))
-acl auch anschauen
-types bekommen wir wieder, type src, url, regex, Destination Domain ect…
-regeln: im squid.conf.g1.txt hat sie darauf hingewiesen, also anschauen, 
  --> acl shopping url_regex -i amazon ebay --> hier auch reihenfolge
	acl tobias src 10.11.111.7
	danach deny tobias
	so eine angabe möchte sie, sie gibt vor wie sie es haben will und wir müssen es schreiben,a ber syntax müssen wir können, also nix vorgeben
--> 



